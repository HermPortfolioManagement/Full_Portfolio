#pragma once

#include "menu.h" // note added when migrating on git: unsure if necessary - may be vestigal.
#include "class.h"
#include "SEC.h"

int construct(int input) { // start runtime funtion
	
	Patient patient(input);

	return safeExit;
	
} // end function
